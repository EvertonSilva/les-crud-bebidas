<%= form_for @stock do |f| %>
  <div>
    <%= f.label :drink_id %>
    <%= f.collection_select(:drink_id, Drink.all, :id, :name) %>
  </div>
  <div>
    <%= f.label :amount %>
    <%= f.number_field :amount %>
  </div>
  <%= f.submit %>
<% end %>